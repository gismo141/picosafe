#!/bin/sh
set -e

case "$1" in
  purge)
  [ ! -L /etc/udev/rules.d/z60_hdparm.rules ] || rm /etc/udev/rules.d/z60_hdparm.rules
  ;;
esac

# Automatically added by dh_installinit
if [ "$1" = "purge" ] ; then
	update-rc.d hdparm remove >/dev/null
fi
# End automatically added section

